import os

SECRET_KEY = '{{ django_secret_key }}'

DEBUG = False

ALLOWED_HOSTS = ['localhost', '127.0.0.1', '{{ application_hostname }}']

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join('{{ sqlite_file_location }}', 'db.sqlite3'),
    }
}

STATIC_ROOT = '{{ nginx_static_dir }}'
